import { styled } from '..'

export const Container = styled('div', {
  display: 'flex',
  flexDirection: 'column',
  minHeight: '100vh',
})

export const Header = styled('header', {
  display: 'flex',
  padding: '2rem 0',
  width: '100%',
  height: '120px',
  maxWidth: 1180,
  margin: '0 auto',
  alignItems: 'center',
})
